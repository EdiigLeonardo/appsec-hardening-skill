# Technology adapters

Adapters are optional implementation helpers.

They MUST NOT define the security model.

An adapter may explain how a conceptual security control commonly appears in a particular language, framework, database, runtime, cloud, or client platform.

The core skill must remain fully usable if this directory is deleted.
