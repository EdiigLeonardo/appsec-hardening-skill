# Trust Boundaries

Map every point where data or authority crosses between trust zones.

Questions:
- Who controls the data?
- Who authenticates the sender?
- Where is authorization enforced?
- What assumptions cross the boundary?
- Can the boundary be bypassed?
