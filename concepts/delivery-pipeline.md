# Delivery pipeline

Review:

source control protections; CI identities; secret access; artifact integrity; deployment approvals; environment separation; production access; rollback; release provenance.

Prefer short-lived workload identity over reusable credentials.
