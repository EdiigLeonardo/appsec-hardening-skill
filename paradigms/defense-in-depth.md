# Defense in Depth

Critical invariants should have independent protective layers where practical.

Avoid multiple controls that all rely on the same untrusted input or claim.

For critical invariants, seek at least two independent controls where practical, e.g. application authorization + data-layer isolation.
