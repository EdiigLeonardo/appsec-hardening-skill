# Zero Trust

Do not implicitly trust a request because it originates from an internal network, known client, trusted service, or authenticated session.

Authenticate and authorize based on explicit evidence and least privilege.
